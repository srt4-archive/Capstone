<% if logged_in %>

<% form_remote_for [@post, Comment.new] do |f| %>
	<p>
		<%= f.label :body, "Comment" %><br />
		<%= f.text_area :body %>
	</p>
	<p>
		<%= f.submit "Add Comment" %>
	</p>
<% end %>

<% end %>
